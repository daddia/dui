{
  "name": "dui-monorepo",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "author": "daddia",
  "packageManager": "pnpm@10.9.0",
  "engines": {
    "node": "^20.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "packages-internal/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "format:check": "prettier --cache --check --ignore-path .gitignore --ignore-path .prettierignore .",
    "format": "prettier --cache --write --list-different --ignore-path .gitignore --ignore-path .prettierignore .",
    "types:check": "turbo run types:check",
    "test": "turbo run test",
    "clean": "turbo run clean",
    "reset": "turbo run clean && turbo run reset && rm -rf node_modules .turbo"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/prettier-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "eslint": "^9.25.1",
    "prettier": "^3.5.3",
    "prettier-plugin-tailwindcss": "^0.6.11",
    "rimraf": "^6.0.1",
    "turbo": "^2.5.1",
    "typescript": "5.8.3",
    "typescript-eslint": "^8.31.0",
    "vitest": "^3.1.2"
  }
}
